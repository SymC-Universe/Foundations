# Substrate Inheritance post-remediation verification against General Cross-Project Research Protocol v0.7.4

**Date:** 2026-09-13  
**Protocol authority:** `General_Cross_Project_Research_Protocol_v0.7.4_FINAL.pdf`, dated 2026-09-12  
**SI branch:** `substrate-inheritance-next`  
**Epistemic status:** P0-Q method/architecture verification. No physical inheritance result is created or promoted by this audit.

## Executive verdict

The GP v0.7.4 SI migration is **implemented and mechanically qualified on the SI-next development branch**. The frozen v0.2 physical-promotion route remains unchanged and authoritative for any physical promotion attempt.

The earlier v0.3 architecture drift identified by the audit has been remediated prospectively in SI-next candidate v0.4. The branch now implements the v0.7.4 distinctions needed for current P0-D/P0-Q work without retroactively changing frozen science.

This is not a P1 confirmation and does not establish a real-system substrate-inheritance result.

## Post-remediation compliance status

### Implemented and qualified

1. **Starting-representation applicability**
   - scalar/modal/conglomeration remain starting representations rather than an exhaustive ontology;
   - `NOT_APPLICABLE` is permitted;
   - an open channel is preserved.

2. **Local identity versus embedded realized behavior**
   - local/intrinsic subsystem identity is reported separately from coupled/embedded behavior;
   - coupling can alter realized behavior without silently rewriting the local object.

3. **Pre-Atlas coordinate independence**
   - Atlas information is downstream/read-only with respect to SI coordinate construction and carrier mapping;
   - lower-level scalar values may not be averaged or conglomerated into a system scalar;
   - no system scalar is required.

4. **Separated epistemic dispositions**
   - existence/admission, participation, observability, identifiability, coordinate estimation, regime placement, uncertainty/tolerance, and final admission/refusal are kept separately auditable.

5. **Hierarchical closure**
   - an explicit closure gate is implemented;
   - exact nested/direct Schur closure and approximate-reduction Function/Limit surfaces have been mapped in FM4;
   - supported, partial, failed, unresolved, and not-applicable states remain available where appropriate.

6. **Recovery/resilience separation**
   - recovery is not equated with state classification;
   - asymptotic return and finite-time amplification are distinct candidate observables where the native dynamics support them;
   - no universal resilience metric is introduced.

7. **Function/Limit balance**
   - `SI_FUNCTION_LIMIT_MAP_PLAN_v0.1.json` defines coequal functioning-interior and limit mapping;
   - FM4 hierarchical closure and FM1 coupling-response mapping have now both been executed as P0-D landscapes rather than boundary-only tests.

8. **Cross-scale recurrence firewall**
   - repeated mathematical motif, preserved architecture across grouping, common physical mechanism, and shared numerical chi region are distinct claim classes;
   - numerical similarity alone is not inheritance or common mechanism.

9. **Experimental Opportunity and Literature Collision Pass**
   - independent experimental ideation and targeted literature-collision records now exist;
   - prior work is used to isolate the residual question rather than retroactively manufacture novelty.

10. **Attribution and residual novelty**
    - nearest-prior-art and residual-contribution ledgers are present;
    - novelty is not granted by the ledger itself.

11. **P0-Q search history**
    - qualification-search history and stopping rationale are preserved rather than hidden behind the current candidate specification.

12. **Run economy and continuity**
    - future substantial/remote physical runs inherit preflight, representative-pilot, bounded-retry, checkpoint/resume, and stale/supersession expectations;
    - small synthetic work is not burdened with unnecessary P2 machinery.

13. **System Model / Engine terminology**
    - the scientific representation is the SI System Model;
    - executable modules are implementations/qualification machinery and do not acquire mechanistic authority from the word Engine.

14. **Rare/extreme proportionality**
    - contrast/limit systems such as H/Ru(0001) may serve as high-information Limit Map probes;
    - rarity alone does not make them representative baselines or privileged centers of interpretation.

## Current Function/Limit investigation status

The frozen P0-D execution order is:

1. `FM4_HIERARCHICAL_CLOSURE_LANDSCAPE` — **COMPLETE**
2. `FM1_COUPLING_RESPONSE_LANDSCAPE` — **COMPLETE**
3. `FM3_LINEAGE_FLOW_LANDSCAPE` — **NEXT**
4. `FM2_EMBEDDING_DEPTH_LANDSCAPE` — pending
5. `FM5_RECOVERY_RESILIENCE_LANDSCAPE` — pending

FM1 and FM4 are synthetic Function/Limit maps. They do not count as physical SI evidence.

## Frozen material not changed

This migration does not rewrite or reinterpret:

- `CORRESPONDENCE_PROTOCOL_v0.2.json`;
- `REAL_SYSTEM_INPUT_SCHEMA_v0.2.json`;
- V1-V10 historical synthetic/mathematical results;
- any Chemistry target route, threshold, or scientific setting;
- physical-threshold status (`false`);
- real-system result status (`NOT_ESTABLISHED`).

Candidate v0.4 remains non-authoritative for physical promotion and cannot rescue, promote, demote, or re-score a target frozen under v0.2.

## Remaining scientific work, not protocol-remediation debt

The principal remaining gaps are scientific rather than GP-migration defects:

- complete FM3 lineage-flow Function/Limit mapping;
- complete FM2 embedding-depth Function/Limit mapping;
- complete FM5 recovery/resilience Function/Limit mapping;
- continue source-readiness monitoring without altering upstream Chemistry routes;
- obtain and ingest an independently admissible real-system parent/child record when one actually exists;
- only then decide whether a P1 inheritance claim is worth freezing under full MFR-14.

## Current claim ceiling

SI remains an active **P0-D / P0-Q investigation** with strong synthetic/mathematical qualification and no established physical substrate-inheritance result. GP v0.7.4 compliance does not promote the science; it makes the current investigation better specified and harder to misinterpret.
